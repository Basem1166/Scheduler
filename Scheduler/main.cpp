#include<fstream>
#include<iostream>

#include"Scheduler.h"

using namespace std;

int main()
{
	Scheduler Schedule;

	int a, b, c, d, e, f, g, h, i, j, k;
	Schedule.Read(a, b, c, d, e, f, g);
	cout << a;

	system("pause");
	return 0;
}